{
  "name": "tutela-txline-activation",
  "private": true,
  "type": "module",
  "description": "Standalone one-time TxLINE devnet activation helper. Deliberately NOT part of the pnpm workspace (see ../../pnpm-workspace.yaml) so it never touches the main lockfile. Run with: npm install && npx tsx activate.ts",
  "scripts": {
    "activate": "tsx activate.ts"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.98.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
